# Copy to `.env.development` for local dev (Vite only loads it in dev mode,
# so the `VITE_BASE_URL` localhost value never leaks into a production build).
# In production, set `GITHUB_TOKEN` via `wrangler secret put GITHUB_TOKEN`.
GITHUB_TOKEN=
VITE_BASE_URL=http://localhost:3000
